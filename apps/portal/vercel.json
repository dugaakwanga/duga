{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate --schema ../../packages/db/prisma/schema.prisma && next build",
  "crons": [
    { "path": "/api/cron/fee-reminders", "schedule": "0 7 * * 1,3,5" }
  ]
}
